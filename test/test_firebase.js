var expect = require('chai').expect,
    mocha = require('mocha'),
    helper = require('../helper');

describe('This is test case for firebase database', function(){
    before(function(done) {
        done();
    });

    // test case puts here

});
